// certs page
let cardMagicGridH = new MagicGrid({
    container: '.m-grid-h',
    animate: false,
    gutter: 32,
    static: true,
    center: true

});
cardMagicGridH.listen();

let faqMagicGridH = new MagicGrid({
    container: '.m-grid-faq-h',
    animate: false,
    gutter: 32,
    static: true,
    center: true

});
faqMagicGridH.listen();

let revMagicGridH = new MagicGrid({
    container: '.m-grid-rev-h',
    animate: false,
    gutter: 32,
    static: true,
    center: true

});
revMagicGridH.listen();
